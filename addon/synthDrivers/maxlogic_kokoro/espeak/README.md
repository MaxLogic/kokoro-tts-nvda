Place the bundled eSpeak-NG runtime here for a standalone build:

- `espeak-ng.exe`
- `libespeak-ng.dll`
- `espeak-ng-data\`

During development, the driver can also fall back to the installed reference add-on assets.
